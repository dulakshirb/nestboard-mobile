import { PropertyType } from "./common";

export type PropertyItem = {
  id: string;
  title: string;
  location: string;
  type: string;
  price: string;
  rating: number;
  image: string;
  lat: number;
  lng: number;
}